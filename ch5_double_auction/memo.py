from posixpath import split
from random import random

from sqlalchemy import false


tmp=[2,2]
tmp.
"".join(tmp)
x=",".join(list(map(str,tmp)))
x_list=x.split(",")
list(map(int,x_list))


def f():
    for i in [1,2,3]:
        for j in [1,2,3]:
            if i==4:
                return [i, j]
tmp=f()
tmp is not None
def f():
    for i in [1,2,3]:
        for j in [1,2,3]:
            if i==3:
                return [i, j]

def f():
    i=1+2
    if i==3:
        return i
f()

buyers=[[1,0],[2,2],[2,1]]
[buyer for buyer in buyers if buyer[0]==2]

tmp=[1,2,3]
tmp2=[2,3,]
tmp_dict=dict(zip(tmp,tmp2))
tmp_str=str(tmp_dict) 
tmp_dict2=eval(tmp_str)
tmp_dict[2]
tmp_dict[3]="a"

[ i for i in tmp_dict.keys() if tmp_dict[i]==3 ]
tmp=[]
random.choice(tmp)
sorted(tmp_dict2.values(), reverse=True)
eval(str(tmp_dict)) 
a=eval("{}") 

[ 1 if len(tmp_dict2.values())==0 else  2]

min(tmp_dict2.values()) if 2==1 else 2

min(tmp_dict2.values())

list(tmp_dict.keys())
min(tmp_dict2.values())


{
    p.id_in_group: dict(
        num_items=p.num_items,
        current_offer=p.current_offer,
        payoff=p.payoff,
        bids=bids,
        asks=asks,
        highcharts_series=highcharts_series,
        news=news,
    )
    for p in players
}


class P:
    A = 1
    B = 2
A1=P
A2=P

A1.A=3

A1.A
A2.A

[a,b]=[A1,A2]
a.A
a=dict(zip(range(1,3),["a","b"]))
import json
["a"]*2
dict(zip(range(1,3),[0] * 10))

a=json.dumps(dict(zip([1,2],[3,4])))
a[2]
a={}
a.values
a.keys
a

a={"a":"b","c":"d"}
a={}
len(a.values)
{i:2 for i in [1,2]}

{p:"" for p in [1,2] }.values()

[i for i in  ]
False